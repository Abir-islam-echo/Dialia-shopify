<!--  Products Area -->
            <div class="tm-section products-area tm-padding-section bg-white">
                <span class="bg-shape-right"><img src="{{ "bg-shape-2.png" | asset_url }}"
                        alt="background shape"></span>
                <div class="container">
                    <div class="row justify-content-center">
                        <div class="col-xl-6 col-lg-8 col-md-10 col-12">
                            <div class="tm-section-title text-center">
                                <h2>{{ section.settings.heading | remove: '<p>' | remove: '</p>' }}</h2>
                                <p>{{ section.settings.description | remove: '<p>' | remove: '</p>' }}</p>
                            </div>
                        </div>
                    </div>
          
                    <div class="row product-slider-active tm-slider-arrow tm-slider-arrow-hovervisible">
                        <!-- Single Product -->
                      {% for product in collection.products %}
                        <h1>{{ product.title }}</h1>
                        {% endfor %}
                      {% for product in collections[section.settings.collection-id].products %}
                          
                            <div class="col">
                            <div class="tm-product">
                                <div class="tm-product-image">
                                    <a class="tm-product-imagelink" href="product-details.html">
                                        <img src="{{ product.featured_image | image_url }}" alt="product image">
                                    </a>
                                    <ul class="tm-product-actions">
                                        <li><button type="button" data-fancybox data-src="#tm-product-quickview"><i
                                                    class="zmdi zmdi-eye"></i></button></li>
                                        <li><a href="cart.html"><i class="zmdi zmdi-shopping-cart"></i></a></li>
                                        <li><a href="cart.html"><i class="zmdi zmdi-favorite"></i></a></li>
                                    </ul>
                                </div>
                                <div class="tm-product-content">
                                    <h5 class="tm-product-title"><a href="{{ product.url }}">{{ product.title }}</a></h5>
                                    <div class="tm-product-rating">
                                        <span class="active"><i class="zmdi zmdi-star"></i></span>
                                        <span class="active"><i class="zmdi zmdi-star"></i></span>
                                        <span class="active"><i class="zmdi zmdi-star"></i></span>
                                        <span class="active"><i class="zmdi zmdi-star"></i></span>
                                        <span class="active"><i class="zmdi zmdi-star"></i></span>
                                    </div>
                                    <h6 class="tm-product-price">{{ product.price | money_with_currency  }}
                                      {% if product.compare_at_price %}<del>{{ product.compare_at_price | money_with_currency  }}</del>{% endif %}</h6>
                                  {% if product.compare_at_price %}
                                    <span class="sale-tag">
                                      Sale!!
                                    </span>
                                  {% endif %}
                                </div>
                            </div>
                        </div>
                          
                    {% endfor %}
                        <!--// Single Product -->
                    </div>
                </div>
            </div>
            <!--//  Products Area -->

{% schema %}
  {
    "name": "Products",
    "settings": [
      {
        "type":"collection",
        "id":"collection-id",
        "label":"collection"
      },
      {
          "type": "richtext",
          "id": "heading",
          "label": "Heading",
          "default": "<p>Welcome to our shop</p>"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description",
          "default": "<p>Lorem ipsum dolor sit amet, in quodsi vulputate pro. Ius illum vocent mediocritatem an cule dicta iriure at phaedrum.</p>"
        },
        {
          "type": "font",
          "id": "font",
          "label": "Font style"
        },
        {
          "type": "color",
          "id": "text-color",
          "label": "Text Color",
          "default": "#000000"
        },
        {
          "type": "range",
          "id": "font-size",
          "min": 12,
          "max": 36,
          "step": 1,
          "unit": "px",
          "label": "Font size",
          "default": 14
        }
      
    ],
    "presets":[
      {
        "name":"products"
      }
    ]
  }
{% endschema %}

{% style %}
  .sale-tag{
    margin: 10px 0;
    padding: 5px 10px;
    background: linear-gradient(-45deg, #1e6a11, #91bb90, #1e6a11);
	background-size: 400% 400%;
	animation: gradient 10s ease infinite;
    display: flex;
    align-items: center;
    color: white;
    font-weight: 600;
    border-radius: 50px;
    justify-content: center;
  }
  @keyframes gradient {
	0% {
		background-position: 0% 50%;
	}
	50% {
		background-position: 100% 50%;
	}
	100% {
		background-position: 0% 50%;
	}
}
{% endstyle %}

{% javascript %}
{% endjavascript %}